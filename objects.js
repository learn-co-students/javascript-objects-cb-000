var playlist = {
  Song: "My Way",
  artistName: "Frank Sinatra"
};

function updatePlaylist(playlist, artistName, title)
{
  return playlist;
}

function removeFromPlaylist(playlist, artistName)
{

delete artistName.dinner;
}
